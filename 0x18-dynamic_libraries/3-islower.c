#include "main.h"

/**
 * _islower - check for lower case letter
 * @c : character to check the case
 * Return:0 for lowercase otherwise 1
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);

}
